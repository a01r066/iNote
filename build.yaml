targets:
  $default:
    builders:
      riverpod_generator:
        options:
          # Use the @riverpod annotation from riverpod_annotation
          riverpod_version: 2
      json_serializable:
        options:
          explicit_to_json: true
          field_rename: snake
